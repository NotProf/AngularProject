import { NoAuthUserDirective } from './no-auth-user.directive';

describe('NoAuthUserDirective', () => {
  it('should create an instance', () => {
    const directive = new NoAuthUserDirective();
    expect(directive).toBeTruthy();
  });
});
