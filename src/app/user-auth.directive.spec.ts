import { UserAuthDirective } from './user-auth.directive';

describe('UserAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new UserAuthDirective();
    expect(directive).toBeTruthy();
  });
});
